# Docker Helper

## Basic commands

`docker run myContainer`

Execute the image 'myContainer' if exists. If the image does not exist docker will look for it in [Docker Hub](https://hub.docker.com/ "Docker Hub").

-----------------------

Test
